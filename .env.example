# PostgreSQL development credentials (change these before sharing an environment)
POSTGRES_DB=delivery_saas
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_local_password

# Backend configuration
JWT_SECRET=replace_with_a_local_development_secret_at_least_32_characters_long
CORS_ALLOWED_ORIGINS=http://localhost:5173

